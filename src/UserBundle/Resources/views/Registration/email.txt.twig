{% block subject %}
    {% autoescape false %}
    {{ 'registration.email.subject'|trans({'%login%': user.username, '%username%': user.fullName, '%password%': pwd, '%confirmationUrl%': confirmationUrl}, 'fos_user_bundle') }}
    {% endautoescape %}
{% endblock %}
{% block body_text %}
{% autoescape false %}
    {{ 'registration.email.message'|trans({'%login%': user.username, '%username%': user.fullName, '%password%': pwd, '%confirmationUrl%': confirmationUrl}, 'fos_user_bundle') }}
    {% endautoescape %}
{% endblock %}
{% block body_html %}{% endblock %}
