{% extends "@App/Base/base.html.twig" %}

{% block page_content %}
        <div class="row top-buffer alert alert-success">
            <h4>{{ 'registration.confirmed'|trans({'%username%': user.username}, 'fos_user_bundle') }}</h4>
            {% if app.session is not empty %}
                {% if targetUrl is not empty %}<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans({}, 'fos_user_bundle') }}</a></p>{% endif %}
            {% endif %}
        </div>
{% endblock %}
